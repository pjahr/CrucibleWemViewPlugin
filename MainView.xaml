<UserControl x:Class="CrucibleWemViewerPlugin.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CrucibleWemViewerPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=True}">

  <Grid>
    <StackPanel>

      <TextBlock Text="Internal file size (bytes):"/>
      <TextBlock Text="{Binding NumberOfBytesInternal.Result}"/>
    
    <TextBlock Text="{Binding NumberOfBytesInternal.IsCompleted}"/>

      <TextBlock Text="Still trying to read raw data (something is broken):"/>
      <TextBlock Text="{Binding NumberOfBytesInternal.IsNotCompleted}"/>
      <TextBlock Text="{Binding NumberOfBytesInternal.Status}"/>

      <Button Content="Click here to convert WEM to OGG"  Command="{Binding ConvertAudioCommand}"/>

      <TextBlock Text="Temp File:"/>
      <TextBlock Text="{Binding OggFilePath}"/>

      <Button Content="Play OGG File"  Command="{Binding PlayAudioCommand}"/>
      
      <TextBlock Text="Playing:"/>
      <TextBlock Text="{Binding Playing}"/>

    </StackPanel>
  </Grid>

</UserControl>
